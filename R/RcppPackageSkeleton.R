# This is all the code needed to add boilerplate files to your project (not created as a package!) To make it into a package.
# Note: This makes the package inside the project you already created, if you created one first. There is no RStudio project (.Rproj).
library(Rcpp)
Rcpp.package.skeleton("RcppPackage")
